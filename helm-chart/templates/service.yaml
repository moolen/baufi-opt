apiVersion: v1
kind: Service
metadata:
  name: {{ include "baufi-optimierer.fullname" . }}
  labels:
    {{- include "baufi-optimierer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "baufi-optimierer.selectorLabels" . | nindent 4 }}
